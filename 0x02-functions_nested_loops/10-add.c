#include "main.h"
#include <stdio.h>

/**
 * main - adds two integers and returns the result.
 *
 * Return: 0.
 */
int main(void)
{
	int result = add(5, 7);
	
	printf("The result of the addition is: %d\n", result);
	return (0);
}

