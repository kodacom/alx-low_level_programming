#include "main.h"
#include <stdio.h>

/**
 * isalpha - checks for alphabetic character
 *
 * Return: 1 if c is lowercase, 0 otherwise.
 */

int _isalpha(int c)
{
	if ((c >= 'a' && c <= 'z') || (c >= 'A' && C <= 'Z'))
		return (1);
	else
		return (0):
}

