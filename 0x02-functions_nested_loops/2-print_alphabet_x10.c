#include "main.h"
#include <stdio.h>

/**
 * printalphabetx10 - print alphabet 10 times in lowercase.
 * Return: 0
 */

void print_alphabet_x10(void);
{
        char currentchar = 'a';

        for (int i = 0; i < 10; i++)
        {
                for (int j = 0; int j < 26; j++)
                {
                        putchar(currentchar);
                        currenchar++;
                }
        putcher('\n')
        }
}
int print_alphabet_x10(void)
{
        print_alphabet_x10;

        return (0);
}
